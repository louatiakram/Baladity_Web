<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">


<title>profile edit data and skills - Bootdey.com</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.0/dist/css/bootstrap.min.css" rel="stylesheet">
<style type="text/css">
    	body{
    background: #f7f7ff;
    margin-top:20px;
}
.card {
    position: relative;
    display: flex;
    flex-direction: column;
    min-width: 0;
    word-wrap: break-word;
    background-color: #fff;
    background-clip: border-box;
    border: 0 solid transparent;
    border-radius: .25rem;
    margin-bottom: 1.5rem;
    box-shadow: 0 2px 6px 0 rgb(218 218 253 / 65%), 0 2px 6px 0 rgb(206 206 238 / 54%);
}
.me-2 {
    margin-right: .5rem!important;
}
/* Custom file input */
.custom-file {
    position: relative;
    display: inline-block;
    width: 100%;
    height: 40px;
    overflow: hidden;
}

.custom-file-input {
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    cursor: pointer;
}

.custom-file-label {
    position: absolute;
    left: 0;
    top: 0;
    width: calc(100% - 32px); /* Adjust the width as needed */
    padding: 0.375rem 0.75rem;
    font-size: 1rem;
    font-weight: 400;
    line-height: 1.5;
    color: #495057;
    background-color: #fff;
    border: 1px solid #ced4da;
    border-radius: 0.25rem;
    cursor: pointer;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}
    </style>
</head>
<body>
<div class="container">
<div class="main-body">
<div class="row">
<div class="col-lg-4">
<div class="card">
<div class="card-body">
<div class="d-flex flex-column align-items-center text-center">
    <div class="d-flex flex-column align-items-center text-center">
        <div class="col-md-6">
            {% if user.getImageUser() %}
                {% set fileExtension = user.getImageUser()|split('.')|last %}
                {% if fileExtension in ['jpg', 'jpeg', 'png', 'gif'] %}
                    <img src="{{ asset('uploads/' ~ user.getImageUser()) }}" class="img-thumbnail" alt="Piece jointe" style="width: 100%; max-width: 300px;"> <!-- Added rounded-circle class -->
                {% endif %}
            {% endif %}
        </div>
    </div> <div class="mt-3">
<h4>{{user.getNomUser}}</h4>
<p class="text-secondary mb-1">{{ user.getTypeUser() }}</p>
</div>
</div>
<hr class="my-4">
</div>
</div>
</div>

<!-- EDIT -->
<div class="col-lg-8">
    <div class="card">
        <div class="card-body">
            <h5 class="d-flex align-items-center mb-3">Edit Profile</h5>
            {{ form_start(form, {'attr': {'class': 'form'}}) }}
            <div class="row mb-3">
                <div class="col-sm-3">
                    <h6 class="mb-0">{{ form_label(form.nom_user) }}</h6>
                </div>
                <div class="col-sm-9 text-secondary">
                    {{ form_widget(form.nom_user, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.nom_user) }}
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-sm-3">
                    <h6 class="mb-0">{{ form_label(form.email_user) }}</h6>
                </div>
                <div class="col-sm-9 text-secondary">
                    {{ form_widget(form.email_user, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.email_user) }}
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-sm-3">
                    <h6 class="mb-0">{{ form_label(form.password) }}</h6>
                </div>
                <div class="col-sm-9 text-secondary">
                    {{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.password) }}
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-sm-3">
                    <h6 class="mb-0">{{ form_label(form.phoneNumber_user) }}</h6>
                </div>
                <div class="col-sm-9 text-secondary">
                    {{ form_widget(form.phoneNumber_user, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.phoneNumber_user) }}
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-sm-3">
                    <h6 class="mb-0">{{ form_label(form.location_user) }}</h6>
                </div>
                <div class="col-sm-9 text-secondary">
                    {{ form_widget(form.location_user, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.location_user) }}
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-sm-3">
                    <h6 class="mb-0">{{ form_label(form.image_user) }}</h6>
                </div>
                <div class="col-sm-9 text-secondary">
                    <div class="custom-file">
                        {{ form_widget(form.image_user, {'attr': {'class': 'custom-file-input', 'id': 'customFile'}}) }}
                        <label class="custom-file-label" for="customFile">Choisir un fichier</label>
                    </div>
                    {{ form_errors(form.image_user) }}
                </div>
            </div>
            <div class="row">
                <div class="col-sm-3"></div>
                <div class="col-sm-9 text-secondary">
                    <button type="submit" class="btn btn-primary px-4">Save Changes</button>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>


<script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.0/dist/js/bootstrap.bundle.min.js"></script>
<script type="text/javascript">
	
</script>
</body>
</html>